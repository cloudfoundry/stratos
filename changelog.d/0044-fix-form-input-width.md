[BugFixes]
- Text fields in the user-invite configuration dialog and the backup password step are full width again, rather than showing about twenty characters at a time. The shared input styling sets border, padding and focus ring but no width, so the field fell back to the browser's default of roughly twenty characters and a client secret or long password scrolled out of sight as it was typed. Nothing was ever cut off; the value was always stored in full (#5758).
