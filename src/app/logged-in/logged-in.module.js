(function () {
  'use strict';

  /**
   * @namespace app.logged-in
   * @memberof app
   * @name logged-in
   * @description Service to store fact is user has logged in
   */
  angular
    .module('app.logged-in', [
      'app.event'
    ]);

})();
