import { AppNameUniqueDirective } from './app-name-unique.directive';

describe('AppNameUniqueDirective', () => {
  it('should create an instance', () => {
    const directive = new AppNameUniqueDirective();
    expect(directive).toBeTruthy();
  });
});
