(function () {
  'use strict';

  /**
   * @namespace app.event
   * @memberof app
   * @name event
   * @description The event bus that allows communication between the
   * view, model and API layers
   */
  angular
    .module('app.event', []);

})();
