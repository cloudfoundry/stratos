import { proxyAPIVersion } from '@stratosui/store';

export const environment = {
  production: true,
  proxyAPIVersion,
  showObsDebug: false,
  disablePolling: true,
  desktopMode: false,
  hideUserMenu: false,
  fixedSideNav: false,
};
