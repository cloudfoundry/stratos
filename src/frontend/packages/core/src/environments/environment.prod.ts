import { cfAPIVersion, proxyAPIVersion } from '@stratosui/store';

export const environment = {
  production: true,
  proxyAPIVersion,
  cfAPIVersion,
  showObsDebug: false,
  disablePolling: true,
  desktopMode: false,
  hideUserMenu: false,
  fixedSideNav: false,
};
