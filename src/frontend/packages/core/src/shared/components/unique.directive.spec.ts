import { UniqueDirective } from './unique.directive';

describe('UniqueDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueDirective();
    expect(directive).toBeTruthy();
  });
});
