import { ApplicationStateIconPipe } from './application-state-icon.pipe';

describe('ApplicationStateIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationStateIconPipe();
    expect(pipe).toBeTruthy();
  });
});
