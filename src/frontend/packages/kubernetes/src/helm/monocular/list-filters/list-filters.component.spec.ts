import { ListFiltersComponent } from './list-filters.component';


describe('Component: ListFilters', () => {
  it('should create an instance', () => {
    const component = new ListFiltersComponent();
    expect(component).toBeTruthy();
  });
});
