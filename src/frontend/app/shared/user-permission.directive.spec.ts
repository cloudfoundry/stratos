import { UserPermissionDirective } from './user-permission.directive';

describe('UserPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new UserPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
