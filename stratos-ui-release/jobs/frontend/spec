---
name: frontend
templates:
    nginx.conf.erb: /var/vcap/jobs/nginx/nginx.conf
    ssl_cert.erb: /var/vcap/jobs/nginx/nginx.crt
    ssl_cert_key.erb: /var/vcap/jobs/nginx/nginx.key

packages:
- nginx
- frontend

properties:
    backend_host:
        description: "Host deploying the backend"
        default: localhost
    backend_port:
        description: "Port the backend is listening on"
        default: 443
    ssl_cert_path:
        description: "SSL certificate path"
    ssl_cert_key_path:
        description: "SSL certificate key path"