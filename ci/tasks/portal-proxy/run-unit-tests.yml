---
platform: linux
inputs:
- name: stratos-ui
image_resource:
  type: docker-image
  source:
   repository: registry.paas-ui:5000/portal-proxy-test
   tag: "latest"
   insecure_registries: [ "registry.paas-ui:5000" ]

run:
  path: sh
  args:
    - -exc
    - |
      cd stratos-ui
      npm install
      npm run test-backend