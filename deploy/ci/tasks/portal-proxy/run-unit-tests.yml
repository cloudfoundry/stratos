---
platform: linux
inputs:
- name: stratos-ui
image_resource:
  type: docker-image
  source:
   repository: registry.capbristol.com:5000/splatform/stratos-proxy-builder:latest
   tag: "latest"
   insecure_registries: [ "registry.capbristol.com:5000" ]

run:
  path: sh
  args:
    - -exc
    - |
      cd stratos-ui
      npm install
      npm run test-backend